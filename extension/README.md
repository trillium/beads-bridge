# Beads Bridge extension

Before loading unpacked: replace `__FUNNEL_HOST__` in `manifest.json`
(host_permissions) and `__FUNNEL_BASE__` in `background.js` / `sidepanel.html`
with your funnel base URL (see repo `.env.example`). committed files never
carry the real hostname.
