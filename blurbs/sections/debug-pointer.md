---
vars:
  BASE: string    # public bridge root
  RESUME: string  # resume bead id, e.g. resumes-zak
---
## if something fails
These pages drop into debug automatically on failure — follow the prefilled block. If a page itself won't load, fetch {BASE}/fetch/{RESUME}/debug and follow it exactly. Do not guess contents. Do not retry blindly more than once.
